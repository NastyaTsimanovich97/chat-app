import { withStyles } from '@material-ui/core/styles';
import ChatInputComponent from './ChatInputComponent';
import styles from './ChatInputComponent.style.js';

export default withStyles(styles)(ChatInputComponent);
